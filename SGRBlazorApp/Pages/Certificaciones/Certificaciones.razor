@page "/certificaciones"
@using SGRBlazorApp.Data
@using SGRBlazorApp.Interfaces
@using CuriousDriveRazorLibrary
@using Blazored.LocalStorage
@using SGRBlazorApp.Shared
@inject ISgrService<Certificacion> bookStoresService
@inject IUserService userService
@inject ILocalStorageService localStorageService
@inject IJSRuntime JSRuntime
@implements IDisposable
@inject NavigationManager navigationManager

<div class="col-12">

    <h3><b>Listado de Configuraciones</b></h3>
    <br />
    <AuthorizeView Roles="Administrador" Policy="SeniorEmployee">
        <Authorized>
            <a class="btn btn-primary" style="text-underline-position:below; cursor:pointer; " href="/addCertificacion">Nueva Certficación</a>
        </Authorized>
    </AuthorizeView>
    <br />
    <br />

    @if (isLoading)
    {
        <RadzenDataGrid AllowFiltering="true" AllowColumnResize="true"
                        FilterMode="FilterMode.Simple" PageSize="20" AllowPaging="true" AllowSorting="true" Data="@CertificacionList" TItem="Certificacion" ColumnWidth="300px"
                        FilterCaseSensitivity="FilterCaseSensitivity.CaseInsensitive"
                        LogicalFilterOperator="LogicalFilterOperator.Or">
            <Columns>
                <RadzenDataGridColumn TItem="Certificacion" Property="Codigo" Title="Codigo" />
                <RadzenDataGridColumn TItem="Certificacion" Property="Descripcion" Title="Descripcion" />
                <RadzenDataGridColumn TItem="Certificacion" Context="sampleBlazorModelsSampleOrder" Title="Editar" Filterable="false" Sortable="false" TextAlign="TextAlign.Center" Width="100px">
                    <Template Context="Certificacion">
                        <RadzenButton Icon="edit" Size="ButtonSize.Medium" Click="@(args => EditRow(Certificacion.Id))" @onclick:stopPropagation="true">
                        </RadzenButton>
                    </Template>

                </RadzenDataGridColumn>
                <RadzenDataGridColumn TItem="Certificacion" Context="order" Filterable="false" Sortable="false" Title="Eliminar"  TextAlign="TextAlign.Center" Width="70px">
                    <Template Context="Certificacion">
                        <RadzenButton ButtonStyle="ButtonStyle.Danger" Icon="close" Size="ButtonSize.Medium" Click="@(args => DeleteRow(Certificacion.Id))" @onclick:stopPropagation="true">
                        </RadzenButton>
                    </Template>
                </RadzenDataGridColumn>
            </Columns>
        </RadzenDataGrid>
        @*<table id="equipos" class="table" style="width:100%">
                <thead>
                    <tr>
                        <th>Codigo</th>
                        <th>Descripcion</th>
                        <th style="width:15%"></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var Certificacion in CertificacionList)
                    {
                        <tr>
                            <td>@Certificacion.Codigo</td>
                            <td>@Certificacion.Descripcion</td>
                            <AuthorizeView Roles="Administrador" Policy="SeniorEmployee">
                                <Authorized>
                                    <td>
                                        <a class="btn btn-success" href=@string.Format("/editCertificacion/{0}", Certificacion.Id)><i class="fa fa-pencil"></i></a>
                                        <a style="background-color:red;color:white" class="btn btn-success" href=@string.Format("/deleteCertificacion/{0}", Certificacion.Id)><i class="fa fa-trash"></i></a>
                                    </td>
                                </Authorized>
                            </AuthorizeView>
                        </tr>
                    }
                </tbody>
            </table>*@
    }
    else
    {
        <div class="col-12" style="background-color: lightgray; height:400px; vertical-align:middle">
            <br /><br />
            <Chase Center="true" Color="#c21f13" Size="70px"></Chase>
        </div>
    }
    @*<SpinLoader IsLoading="isLoading">
            <LoadingTemplate>
                <div class="col-12" style="background-color: lightgray; height:400px; vertical-align:middle">
                    <br /><br />
                    <Chase Center="true" Color="#c21f13" Size="70px"></Chase>
                </div>
            </LoadingTemplate>
            <ContentTemplate>
                @if (filteredCertificacionList != null)
                {
                    <GridView Items="filteredCertificacionList" ReloadList="IsGridViewFiltered">
                        <GridColumns>
                            <GridColumn ColumnTitle="NumeroCertificacion"
                                        OnSearchTextChanged="@(e => OnCertificacionearchTextChanged(e, "NumeroCertificacion"))"></GridColumn>
                            <GridColumn ColumnTitle="Modelo"
                                        OnSearchTextChanged="@(e => OnCertificacionearchTextChanged(e, "Modelo"))"></GridColumn>
                            <GridColumn ColumnTitle="Certificacion"
                                        OnSearchTextChanged="@(e => OnCertificacionearchTextChanged(e, "Certificacion"))"></GridColumn>
                            <AuthorizeView Roles="Administrador" Policy="SeniorEmployee">
                                <Authorized>
                                    <GridColumn ColumnTitle="Actions"></GridColumn>
                                </Authorized>
                            </AuthorizeView>
                        </GridColumns>
                        <GridRow Context="Certificacion">
                            <td>
                                <NavLink href=@string.Format("/Certificacion/Certificaciondetail/{0}", @Certificacion.Id)>
                                    @Certificacion.Id
                                </NavLink>
                            </td>

                                <Authorized>
                                    <th>
                                        <a style="text-underline-position:below; cursor:pointer; color:blue" href=@string.Format("/editCertificacion/{0}", Certificacion.Id)>Edit</a> |
                                        <a style="text-underline-position:below; cursor:pointer; color:blue" @onclick="(() => DeleteCertificacion(Certificacion.Id))">Delete</a>
                                    </th>
                                </Authorized>
                            </AuthorizeView>
                        </GridRow>
                    </GridView>
                }
            </ContentTemplate>
        </SpinLoader>*@
</div>

@code {

    public Certificacion Certificacion { get; set; }
    public List<Certificacion> CertificacionList { get; set; }
    public List<Certificacion> filteredCertificacionList { get; set; }
    public string SelectedCity { get; set; }

    public bool IsVisible { get; set; }
    public bool Result { get; set; }
    public string RecordName { get; set; }
    public string[] Cities { get; set; }
    public bool IsGridViewFiltered { get; set; }
    bool isLoading = false;

    ElementReference firstNameTextBox;

    void EditRow(Guid id)
    {
        navigationManager.NavigateTo(string.Format("/editCertificacion/{0}", id));
    }

    void DeleteRow(Guid id)
    {
        navigationManager.NavigateTo(string.Format("/deleteCertificacion/{0}", id));
    }


    protected override void OnInitialized()
    {
        Console.WriteLine("Certificacion - OnInitialized");
        base.OnInitialized();
    }

    [CascadingParameter]
    private Task<AuthenticationState> authenticationStateTask { get; set; }
    ClaimsPrincipal user;

    bool IsUserAuthenticated;
    bool IsPublisher;
    bool IsSeniorEmployee;

    public int Count { get; set; } = 400;

    private SGRBlazorApp.Data.User users;

    protected override async Task OnInitializedAsync()
    {
        user = (await authenticationStateTask).User;

        if (!user.Identity.IsAuthenticated)
        {
            navigationManager.NavigateTo("/");
        }
        Console.WriteLine("Certificacion - OnInitializedAsync");

        Certificacion = new Certificacion();
        CertificacionList = filteredCertificacionList = await bookStoresService.GetAllAsync("Certificacion/GetCertificacion");

        if (CertificacionList == null)
        {

            RefreshRequest refreshRequest = new RefreshRequest();
            refreshRequest.AccessToken = await localStorageService.GetItemAsync<string>("accessToken");
            refreshRequest.RefreshToken = await localStorageService.GetItemAsync<string>("refreshToken");
            var user1 = await userService.RefreshTokenAsync(refreshRequest);
            await localStorageService.SetItemAsync("accessToken", user1.AccessToken);

            CertificacionList = filteredCertificacionList = await bookStoresService.GetAllAsync("Certificacion/GetCertificacion");
        }
        isLoading = true;

        //await JSRuntime.InvokeAsync<object>("TestDataTablesAdd", "#equipos");
        await base.OnInitializedAsync();
    }

    protected override void OnParametersSet()
    {
        Console.WriteLine("Certificacion - OnParametersSet");
        base.OnParametersSet();
    }

    protected override async Task OnParametersSetAsync()
    {
        Console.WriteLine("Certificacion - OnParametersSetAsync");
        await base.OnParametersSetAsync();
    }

    protected override bool ShouldRender()
    {
        base.ShouldRender();
        Console.WriteLine("Certificacion - ShouldRender");

        return true;
    }

    protected override void OnAfterRender(bool firstRender)
    {
        Console.WriteLine("Certificacion - OnAfterRender - firstRender = " + firstRender);
        base.OnAfterRender(firstRender);
    }

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        Console.WriteLine("Certificacion - OnAfterRenderAsync - firstRender = " + firstRender);
        ////await JSRuntime.InvokeAsync<object>("TestDataTablesRemove", "#equipos");
        //await JSRuntime.InvokeAsync<object>("TestDataTablesAdd", "#equipos");
        if (firstRender)
        {
            //await LoadCertificacion();

            //isLoading = false;
            StateHasChanged();
        }

        await base.OnAfterRenderAsync(firstRender);
    }

    public void Dispose()
    {
        Console.WriteLine("Certificacion - Dispose");
    }

    private async Task LoadCertificacion()
    {
        await Task.Delay(500);

        CertificacionList = filteredCertificacionList = await bookStoresService.GetAllAsync("Certificaciones/GetCertificaciones");

        if (CertificacionList == null)
        {

            RefreshRequest refreshRequest = new RefreshRequest();
            refreshRequest.AccessToken = await localStorageService.GetItemAsync<string>("accessToken");
            refreshRequest.RefreshToken = await localStorageService.GetItemAsync<string>("refreshToken");
            var user1 = await userService.RefreshTokenAsync(refreshRequest);
            await localStorageService.SetItemAsync("accessToken", user1.AccessToken);

            CertificacionList = filteredCertificacionList = await bookStoresService.GetAllAsync("Certificaciones/GetCertificaciones");
        }

        isLoading = true;
        //await JSRuntime.InvokeAsync<object>("TestDataTablesRemove", "#equipos");
        //await JSRuntime.InvokeAsync<object>("TestDataTablesAdd", "#equipos");

        StateHasChanged();
    }



    private async Task DeleteCertificacion(Guid CertificacionId)
    {
        var confirmado = await JSRuntime.Confirmar("Atencion!", "¿Desea eliminar la Certficación?", TipoMensajeSweetAlert.warning);
        if (confirmado)
        {
            await bookStoresService.DeleteAsync("Certificaciones/DeleteCertificaciones/", CertificacionId);
            await LoadCertificacion();
        }
        //throw new Exception("DeleteCertificacion");
    }

    private void EditCertificacion(Certificacion argCertificacion)
    {
        Certificacion = argCertificacion;
    }

    private void OnSelectCityChange(ChangeEventArgs changeEventArgs)
    {
        SelectedCity = (string)changeEventArgs.Value;
    }

    private void OnCertificacionearchTextChanged(ChangeEventArgs changeEventArgs, string columnTitle)
    {
        //string searchText = changeEventArgs.Value.ToString();
        //IsGridViewFiltered = true;

        //switch (columnTitle)
        //{
        //    case "NumeroCertificacion":
        //        filteredCertificacionList = CertificacionList.Where(auth => auth.NumeroCertificacion.ToLower().Contains(searchText)).ToList();
        //        break;
        //    case "Modelo":
        //        filteredCertificacionList = CertificacionList.Where(auth => auth.Modelo.ToLower().Contains(searchText)).ToList();
        //        break;
        //    case "Certificacion":
        //        filteredCertificacionList = CertificacionList.Where(auth => auth.Certificacion.ToLower().Contains(searchText)).ToList();
        //        break;
        //    default:
        //        Console.WriteLine("Default case");
        //        break;
        //}
    }

}